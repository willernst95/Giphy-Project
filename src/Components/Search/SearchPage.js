import React from "react";

const SearchPage = (props) => {
    // Inputs / Button / Function (here or elsewhere to trigger the fetch call)
    return <h1>SearchPage Works</h1>
}

export default SearchPage